#include <stdio.h>
    
int main(int argc, char *argv[])
{
    int key = 2;
    switch(key)
        for (int i = 0; i < 10; ++i) {
            key = i;
        case 2: printf("case 2: key is %d\n", key);
        case 4: printf("case 4: key is %d\n", key);
        }
    return 0;
}
